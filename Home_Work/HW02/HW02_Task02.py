users_list = input(f"Введите значения через запятую: ")


values_list = users_list.replace(" ", "").split(",")

n = 0


while n <= len(values_list) - 1:
    values_list[n]
    n += 1

print(values_list)

# Не получилось